using UnityEngine;
using System.Collections;

public class TestForce : MonoBehaviour {

	Rigidbody rb;

	// Use this for initialization
	void Start () {	
		//rb = GetComponent<Rigidbody> ();
		//rb.AddForce (new Vector3(-200,1,0));

	}
	
	// Update is called once per frame
	void Update () {
	}
}
